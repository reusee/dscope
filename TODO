final type, 不可覆盖的类型

accumulator
  sub重定义时，并不覆盖原来的值，而是保留成指针定义
  也就是保留父scope的值，例如一些扫尾回调，就需要保证sub后仍然生效

support cyclic decl

checker:
  scoped variable captured by closure

dscope的并发更新
一，直接堆叠union map，不复制
一，直接生成多个scope，需要的时候再合并

